# Requirements for APPS Model Analysis
# Install with: pip install -r requirements.txt

# Core dependencies
torch>=2.0.0
transformers>=4.35.0
datasets==3.6.0
accelerate>=0.24.0

# For JSON handling (usually built-in but explicit)
# json is built-in

# Optional but recommended
tqdm>=4.65.0  # Progress bars
sentencepiece>=0.1.99  # For some tokenizers
protobuf>=3.20.0  # For some tokenizers

# Development
ipython>=8.12.0  # For interactive testing
jupyter>=1.0.0  # For notebook analysis
